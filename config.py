"""Все настройки приложения в одном месте."""

# --- анализ ---
MODEL_PATH = "model.onnx"
THRESHOLD = 0.7          # порог, выше которого участок считается подозрительным
WINDOW = 2.0             # длина окна анализа, с
HOP = 0.5                # шаг окна, с

# --- сервис SRW ---
# Пароль лежит прямо здесь — так удобнее для демо, но в общий репозиторий
# такой файл коммитить не стоит.
BASE_URL = "http://localhost:8000"
USERNAME = "demo"
PASSWORD = "demo"

# Сервер лимитирует: 10 запросов в минуту для ADMIN и USER, 120 для SERVICE.
POLL_INTERVAL = 6.0      # пауза между опросами результата, с
RETRY_PAUSE = 15.0       # пауза после 429, с
RESULT_TIMEOUT = 180.0   # сколько всего ждём вердикт, с

# Модель на сервере по умолчанию: pytorch или pyara
DEFAULT_MODEL = "pytorch"

# Потоковый режим: воркер ждёт 16 кГц моно int16.
STREAM_SR = 16000
CHUNK_SECONDS = 0.5
STREAM_TIMEOUT = 300.0   # сколько всего ждём ответы потокового воркера, с
STREAM_IDLE = 20.0       # сколько ждём тишины, прежде чем считать поток законченным, с
